"""Crossband: a group chat with several AI models at once.

The frontend carries the same version in frontend/package.json; both move
together at a release, since a user runs one application rather than two.
"""

__version__ = "0.2.0"
